const numbers = [10, 20, 30, 40, 50, 60, 70, 80];

// 3 parameter, first parameter item, second parameter index number and third parameter all parameter
numbers.map((item, index, arr)=>{
    console.log('item', item, 'index Number', index, 'array', arr);
});